<Application x:Class="StartDX.Dock.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Intentionally empty: ThemeManager.Initialize() builds the [contract | theme | controls] dictionary stack
         in code so the swappable theme slot has a fixed, known index. No StartupUri: App.OnStartup wires services first. -->
    <Application.Resources/>
</Application>
